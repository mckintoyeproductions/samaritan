rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEV ONLY: any signed-in user can read/write ride requests
    match /ride_requests/{id} {
      allow read, write: if request.auth != null;
    }
  }
}
